<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>dev.dashaun</groupId>
    <artifactId>active-active-demo-parent</artifactId>
    <version>0</version>
    <packaging>pom</packaging>
    <name>${project.groupId}.${project.artifactId}</name>

    <properties>
        <java.version>25</java.version>
        <spring-cloud.version>2025.0.0</spring-cloud.version>
        <gemfire.version>10.1.4</gemfire.version>
        <spring-boot-gemfire.version>2.0.0</spring-boot-gemfire.version>
    </properties>

    <modules>
        <module>publish</module>
        <module>gateway</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.vmware.gemfire</groupId>
                <artifactId>gemfire-core</artifactId>
                <version>${gemfire.version}</version>
            </dependency>
            <dependency>
                <groupId>com.vmware.gemfire</groupId>
                <artifactId>spring-boot-3.5-gemfire-10.1</artifactId>
                <version>${spring-boot-gemfire.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
